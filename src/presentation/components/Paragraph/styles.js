import styled from 'styled-components'

import { defaultClasses } from '../'

export const StyledParagraph = styled.p`
  margin-bottom: 1rem;
  font-size: 2rem;
  color: #000;
  display: flex;

  ${defaultClasses}
`
